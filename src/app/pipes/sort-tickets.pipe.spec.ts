import { SortTicketsPipe } from './sort-tickets.pipe';

describe('SortTicketsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTicketsPipe();
    expect(pipe).toBeTruthy();
  });
});
